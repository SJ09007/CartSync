# Server Configuration
PORT=5000

# PostgreSQL Database Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_postgres_password
PGDATABASE=cartsync_db

# Redis Configuration (defaults to localhost:6379 if not set)
REDIS_URL=redis://localhost:6379
